/* === Search / Filter Module === */
// Search functionality is integrated into App but exposed here for organization
const Search = {
  // Filters are managed by App.filterJobs which is called on input
  // Advanced search is handled by UI.showAdvancedSearch()
};
